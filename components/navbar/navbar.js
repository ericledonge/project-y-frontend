const NavBar = () => (
  <div className="NavBar" style={navBarStyle}>
    NAVBAR
  </div>
);

const navBarStyle = {
  backgroundColor: "red",
  color: "white",
  width: "100%",
  height: "60px",
};

export default NavBar;
